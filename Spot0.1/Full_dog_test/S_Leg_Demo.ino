/*void S_Leg_Demo()
{
  for(X=0; X>-100; X--)
  {
    swing_2D(X, Y_rest, Z_rest);
    delay(d_t1);
  }
  for(X=-100; X<100; X++)
  {
    swing_2D(X, Y_rest, Z_rest);
    delay(d_t1);
  }
  for(X=100; X>-100; X--)
  {
    swing_2D(X, Y_rest, Z_rest);
    delay(d_t1);
  }
  for(X=-100; X<0; X++)
  {
    swing_2D(X, Y_rest, Z_rest);
    delay(d_t1);
  }
  
  delay(100);
   
  for(Y=-220; Y>-270; Y--)
  {
    swing_2D(X_rest, Y, Z_rest);
    delay(d_t1);
  }
  for(Y=-270; Y<-160; Y++)
  {
    swing_2D(X_rest, Y, Z_rest);
    delay(d_t1);
  }
  for(Y=-160; Y>-270; Y--)
  {
    swing_2D(X_rest, Y, Z_rest);
    delay(d_t1);
  }
  for(Y=-270; Y<-220; Y++)
  {
    swing_2D(X_rest, Y, Z_rest);
    delay(d_t1);
  }

  delay(100);

  for(Z=64; Z>-10; Z--)
  {
    swing(X_rest, Y_rest, Z);
    delay(d_t1);
  }
  for(Z=-10; Z<150; Z++)
  {
    swing(X_rest, Y_rest, Z);
    delay(d_t1);
  }
  for(Z=150; Z>-10; Z--)
  {
    swing(X_rest, Y_rest, Z);
    delay(d_t1);
  }
  for(Z=-10; Z<64; Z++)
  {
    swing(X_rest, Y_rest, Z);
    delay(d_t1);
  }
  
  delay(100);
}*/
