/*void llright()
{
  swing(x1, y2, z1);
    
    delay(d_time);
    
    for(X=x1; X>=x2; X--)
    {
      Z=-dzdx*X+Z_rest;     
      swing(X, y2, Z);
      delay(d_t1);
    }

    delay(d_time);

    for(Y=y2; Y<=y3; Y++)
    {
      swing(x2, Y, z1);
      delay(d_t1);
    }

    delay(d_time);

    for(X=x2; X<=x_mid; X++)
    {
      Z=-dzdx*X+Z_rest;     
      swing(X, y3, Z);
      delay(d_t1);
    }

    delay(d_time);

    for(X=x_mid; X<=x1; X++)
    {
      Y=-dydx*X+Y_rest+y_offset;
      Z=-dzdx*X+Z_rest;
      swing(X, Y, Z);
      delay(d_t1);
    }
}*/
